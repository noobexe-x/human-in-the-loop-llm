services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      FLASK_RUN_HOST: 0.0.0.0
      PORT: 5000
    volumes:
      - ./instance:/app/instance
    restart: unless-stopped
